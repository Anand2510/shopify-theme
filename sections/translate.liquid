{% form 'localization', id:"localization_form_tag", class:"dropup" %}
    <div class="localeBtn">{{ 'globe.png' | asset_img_url: '50px' | img_tag  }}</div>
    <div class="dropup-content">
        {% for locale in shop.published_locales %}
            {% comment %} <a href="">{{ locale.iso_code }}</a> {% endcomment %}
            <a href="#" id="localItem" lang="{{ locale.iso_code }}">{{ locale.endonym_name }}</a>
        {% endfor %}
    </div>
    <input type="hidden" id="localeCode" name="locale_code" value="{{ form.current_locale.iso_code }}">
    {% endform %}